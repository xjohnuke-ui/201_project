<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<html>
<head>
    <title>Product Management</title>
    <style>
        .admin-container { padding: 20px; }
        .action-bar { margin-bottom: 20px; }
        .btn-add { padding: 10px 20px; background: #4CAF50; color: white; text-decoration: none; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .action-btn { padding: 5px 10px; margin: 0 5px; text-decoration: none; }
        .btn-edit { background: #2196F3; color: white; }
        .btn-delete { background: #f44336; color: white; }
    </style>
</head>
<body>
    <div class="admin-container">
        <h1>Product Management</h1>
        
        <div class="action-bar">
            <a href="admin/goods?action=add" class="btn-add">Add Product</a>
        </div>
        
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Product Name</th>
                    <th>Image</th>
                    <th>Price</th>
                    <th>Category</th>
                    <th>Manufacturer</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <c:forEach var="goods" items="${goodsList}">
                    <tr>
                        <td>${goods.goodsId}</td>
                        <td>${goods.goodsName}</td>
                        <td>
                            <img src="${pageContext.request.contextPath}/goodsPic/${goods.pic}" 
                                 alt="${goods.goodsName}" width="50" height="50">
                        </td>
                        <td>¥ ${goods.price}</td>
                        <td>${goods.type}</td>
                        <td>${goods.manufacturer}</td>
                        <td>
                            <a href="admin/goods?action=edit&id=${goods.goodsId}" class="action-btn btn-edit">Edit</a>
                            <a href="admin/goods?action=delete&id=${goods.goodsId}" 
                               class="action-btn btn-delete"
                               onclick="return confirm('Are you sure you want to delete ${goods.goodsName}?')">Delete</a>
                        </td>
                    </tr>
                </c:forEach>
            </tbody>
        </table>
    </div>
</body>
</html>
